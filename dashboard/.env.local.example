# API origin only (no /api/v1 suffix — the app appends /api/v1/... itself).
NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional: landing + footer GitHub link (your real repo URL)
# NEXT_PUBLIC_GITHUB_URL=https://github.com/your-org/tracedog

# Traces → admin panel: paste the API’s ADMIN_API_KEY (TraceDog-backend .env or Render), not a Vercel env var.
